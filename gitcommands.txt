thoery-- Q&N                    Please Subscribe to Channel. Follow Ig- Divyansh_2207
flow
git locally
github

-------------------------

what is git?
git is tool. to keep or to manage source code.

what is github/gitlab?
these are cloud repository. to keep the records or the source code.

why we cant use google drive?
git--it helps in version control system.

--central versioning
--distributed versioning

--------------------------

Commands (one time for initial setup)

1) git config --global user.name "yourusername"  
2) git config --global user.email "youremail"
3) git config --list 

--------------------------

Most used Commands --

git init (initialize main branch)
git version (to check the version)
ls (list files in folder)
git status( to check file/folder status)
mkdir foldername( to make folder through bash)
nano filename (to make file through bash)
git add .
git status
git log ( to check commit made by us)
git branch( to check the branches)
git branch branchname ( to make a new branch with name branchname)
git checkout branchname (to jump form one branch to another branch)
git log --oneline ( commits in one line)
git commit -m "comment" (to make a new commit)
git merge branchname ( for merging the branches)

----------------------

to use the github and push the code.

step1 ) make the account in github
step2 ) click create new repository and provide the name.

step3 git remote add origin path
git branch -M main
git push -u origin main





